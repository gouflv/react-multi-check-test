import styled from 'styled-components';
import {View} from '../components/View/View';

const MultiCheckPanel = styled(View)`
  padding: 4px;
  background-color: #fff;
`;

MultiCheckPanel.displayName = 'MultiCheckPanel';

export default MultiCheckPanel;
